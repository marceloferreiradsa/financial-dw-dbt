# Copie para .env.airflow e preencha os valores.
# Este arquivo É versionado (sem valores reais).
# O .env.airflow NÃO é versionado (está no .gitignore).

# ── Segurança do Airflow ──────────────────────────────────────────────────────
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=

# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW_SECRET_KEY=

# ── Credenciais do admin (primeira inicialização) ─────────────────────────────
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin
AIRFLOW_ADMIN_EMAIL=admin@example.com

# ── Configuração de dados ─────────────────────────────────────────────────────
# Caminho do DuckDB DENTRO do container (mapeado pelo volume duckdb-data)
DUCKDB_PATH=/opt/airflow/data/financial_dw.duckdb

# Caminho do projeto dbt DENTRO do container (mapeado pelo bind mount)
DBT_PROJECT_DIR=/opt/airflow/dbt_project

# Parâmetros de ingestão
BACEN_START_DATE=2020-01-01
TICKERS=PETR4.SA,VALE3.SA,ITUB4.SA,BBDC4.SA,WEGE3.SA,MGLU3.SA
