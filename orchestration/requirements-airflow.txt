# Apenas dependencias do Airflow — dbt e scripts de ingestao
# ficam no venv isolado /home/airflow/dbt-env (criado no Dockerfile).
# Essa separacao evita conflitos de dependencia entre Airflow e dbt,
# conforme recomendado pela documentacao do Astronomer Cosmos.

# Cosmos: integracao Airflow + dbt com task-level observability
astronomer-cosmos==1.8.0

# Provider SQL generico (usado pelo Cosmos internamente)
apache-airflow-providers-common-sql==1.14.0
