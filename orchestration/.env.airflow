# Copie para .env.airflow e .env (ambos no .gitignore).
# .env.airflow: injeta variaveis no container
# .env: interpolacao de variaveis no docker-compose.yml
# Ambos devem ter o mesmo conteudo.

# -- Seguranca do Airflow ------------------------------------------------------
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=mj3bN7reR580rVNz0YmBBgovsuNKN54OBaR8g7vglhs=

# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW_SECRET_KEY=857b9b2fb58233539ef22e3e7da282ff893d45743b52f833aa878766ddd93e95

# -- Credenciais do admin (primeira inicializacao) -----------------------------
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin
AIRFLOW_ADMIN_EMAIL=admin@example.com

# -- Configuracao de dados -----------------------------------------------------
DUCKDB_PATH=/opt/airflow/data/financial_dw.duckdb
DBT_PROJECT_DIR=/opt/airflow/dbt_project
BACEN_START_DATE=2020-01-01
TICKERS=PETR4,MGLU3,VALE3,ITUB4
